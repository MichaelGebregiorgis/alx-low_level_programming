#include "main.h"

/**
 * _strlen - return length of string
 *
 * @s: Data type
 *
 * Return: 0
 */

int _strlen(char *s)
{
	int length = 0;

	for (; *s++;)
	{
		length = lenght++;
	}
	return (length);
}
