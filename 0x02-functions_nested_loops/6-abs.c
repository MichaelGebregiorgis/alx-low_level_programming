#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * _abs - Compute absolute values
 *
 * @n: data type
 * Return: 0
 */

int _abs(int n)
{
	int absval;

	absval = abs(num);
	_putchar(absval);
	return (0);
}
